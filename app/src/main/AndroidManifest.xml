<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.markus.customdictionary">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_customdictionary"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddWordActivity"
            android:label="Add a word"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dictionary_use"
            android:label="Search for words"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".groupByActivity"
            android:label="Browse words"
            android:parentActivityName=".MainActivity">
            >
        </activity>
        <activity
            android:name=".web_activity"
            android:label="@string/title_activity_web_activity"
            android:parentActivityName=".Dictionary_use">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.markus.customdictionary.Dictionary_use" />
        </activity>
        <activity android:name=".Training"
            android:label="Training"
            android:parentActivityName=".MainActivity"></activity>
    </application>

</manifest>